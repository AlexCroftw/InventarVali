@model List<Computer>
<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient m-lg-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-black py-2">Computer List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
    </div>
    <div class="row pb-3">
        <div class="col-12">
        </div>
        <div class="text-center">
            <a asp-controller="Computer" asp-action="CreateComputer" class="btn btn-primary">
                <i class="bi bi-plus-square-fill"></i> Create New Computer
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>
                    Type
                </th>
                <th>
                    Model
                </th>
                <th>
                    Description
                </th>
                <th>
                    Serial Number
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>
                        @obj.Type
                    </td>
                    <td>
                        @obj.Model
                    </td>
                    <td>
                        @obj.Description
                    </td>
                    <td>
                        @obj.SerialNumber
                    </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Computer" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>Edit
                            </a>
                            <a asp-controller="Computer" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash3"></i>Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>
